# Local CPU analysis and paper-figure environment (2026-08-25)
numpy==1.26.4
scipy==1.11.4
matplotlib==3.6.3
h5py==3.10.0
torch==2.12.0
pillow==12.2.0

# GPU training is recorded separately in requirements-training-cuda.txt.
# Walrus inference additionally follows the public PolymathicAI/walrus stack;
# it is isolated from the matched baseline environment.
